# Blobb

Contrast Palette Checker is a small React/Vite app for testing color palette contrast against WCAG readability guidelines.

## Project

The app lives in:

```bash
contrast-palette-checker
```

## Run Locally

```bash
cd contrast-palette-checker
npm install
npm run dev
```

## Build

```bash
cd contrast-palette-checker
npm run build
```
